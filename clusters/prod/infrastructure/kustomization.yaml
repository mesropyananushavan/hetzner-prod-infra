apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# network-policies тут намеренно нет: это copy-paste шаблон для новых приложений
# (namespace example-prod не существует), рабочий пример — apps/smart-rest.
resources:
  - cert-manager
  - traefik

