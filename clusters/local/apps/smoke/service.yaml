apiVersion: v1
kind: Service
metadata:
  name: smoke-web
  labels:
    app.kubernetes.io/name: smoke-web
spec:
  selector:
    app.kubernetes.io/name: smoke-web
  ports:
    - name: http
      port: 80
      targetPort: http

